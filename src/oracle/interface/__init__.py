"""Interface layer — HTTP API, WebSocket gateway, request/response schemas.

Contains: FastAPI routers, middleware, DI setup, WebSocket gateway.
Depends on: application layer (use cases) and infrastructure (via DI).
"""
