# Install alongside requirements.txt:
# pip install -r requirements.txt -r requirements-dev.txt

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
anyio>=4.3.0             # async test support
httpx>=0.27.0            # AsyncClient for API tests
asgi-lifespan>=2.1.0     # triggers ASGI lifespan in tests (httpx ASGITransport doesn't)

# ── Integration Testing ───────────────────────────────────────────────────────
testcontainers>=4.4.0    # real PostgreSQL + Redis containers for integration tests

# ── Code Quality ──────────────────────────────────────────────────────────────
ruff>=0.4.0
black>=24.0.0
mypy>=1.9.0
pre-commit>=3.7.0
